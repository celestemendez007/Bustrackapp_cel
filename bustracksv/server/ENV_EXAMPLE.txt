# Copia este archivo a .env y completa con tus credenciales

# Firebase Admin SDK Configuration
# Opción 1: Ruta al archivo JSON de credenciales de servicio
GOOGLE_APPLICATION_CREDENTIALS=./config/firebase-key.json

# Opción 2: Credenciales como JSON string (alternativa a GOOGLE_APPLICATION_CREDENTIALS)
# FIREBASE_CREDENTIALS='{"type":"service_account","project_id":"your-project-id",...}'

# Firebase Project ID (opcional, se detecta automáticamente desde las credenciales)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Database Configuration (existing)
DATABASE_URL=postgresql://user:password@localhost:5432/bustracksv

# JWT Secret (existing)
JWT_SECRET=your-secret-key-here

# Server Configuration (existing)
PORT=4000

# Google Maps API Key (Required for Route Generation)
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

